const path  = require('path');
const util = require('util');//using this module it will give us the time and date when the directry was accessed

const dirname = path.join(__dirname); //this will give us the base directory of current file
util.log(dirname)
